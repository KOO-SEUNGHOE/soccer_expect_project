# 2026 대시보드 디자인 트렌드(다크모드 기본, 절제된 팔레트, 순수 검정 대신
# 짙은 회색 표면)를 반영한 테마. dashboard/app.py의 커스텀 CSS와 함께 쓴다.
[theme]
base = "dark"
primaryColor = "#22c55e"
backgroundColor = "#0f0f16"
secondaryBackgroundColor = "#1a1a24"
textColor = "#e5e7eb"
font = "sans serif"
